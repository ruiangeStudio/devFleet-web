# 2026-03-28

## 完成：网站多语言 (i18n) 实现

- 安装 vue-i18n v10
- 创建 `src/i18n/zh.ts`、`src/i18n/en.ts`、`src/i18n/index.ts`
- 创建 `src/composables/useLocale.ts`（toggleLocale + localStorage 持久化）
- 更新 `main.ts` 注册 i18n 插件
- 全部 6 个组件（NavBar / HeroSection / FeaturesSection / ScreenshotsSection / CtaSection / FooterSection）完成国际化
- NavBar 加入语言切换按钮（地球仪图标 + EN/中标签）

## 追加：新增日文 (ja) 支持

- 创建 `src/i18n/ja.ts`，完整翻译所有文本（nav / hero / features / screenshots / cta / footer）
- 更新 `src/i18n/index.ts`，注册日文并扩展 `Locale` 类型为 `'zh' | 'en' | 'ja'`
- 更新 `src/composables/useLocale.ts`，三语言循环切换 zh→en→ja→zh
- NavBar 语言按钮改为计算属性驱动，显示 `中`/`EN`/`JA`，tooltip 显示下一语言名称
- 构建通过（410ms）
